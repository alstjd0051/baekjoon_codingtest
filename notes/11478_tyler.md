# 11478 풀이

모든 부분 문자열을 직접 `set`에 넣어도 되지만, 문자열 객체를 대량으로 저장하면 메모리 부담이 커질 수 있다.

그래서 각 부분 문자열을 **이중 롤링 해시** `(hash1, hash2)`로 변환해서 집합에 넣는다.

- 접두사 해시와 거듭제곱 배열을 미리 계산
- 구간 `[i, j)`의 해시를 O(1)에 계산
- 모든 `i, j` 쌍에 대해 해시쌍을 `set`에 넣고 크기를 출력

시간 복잡도는 `O(N^2)`, 공간 복잡도는 서로 다른 부분 문자열 수에 비례한다.
