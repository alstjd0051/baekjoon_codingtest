# 3568 풀이

공통 타입은 첫 토큰(`base`)으로 두고, 각 변수 토큰을 따로 파싱해서 한 줄씩 다시 출력하면 된다.

- 변수 토큰에서 알파벳 연속 구간은 변수명
- 나머지는 변수별 추가 타입(`*`, `&`, `[]`)
- 추가 타입은 오른쪽에 붙어 있으므로 뒤집어서 `base` 뒤에 붙여야 함
- 단, 배열 `[]`는 뒤집을 때 `][`가 되면 안 되므로 항상 한 쌍으로 처리

입력 길이가 짧아서 문자열 처리만으로 충분하다.
